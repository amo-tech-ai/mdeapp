import { ChatPageClient } from "./chat-page-client";

/** Concierge surface — v2-only after SAN-891 · CK-V2-005 — Retire @copilotkit/react-ui. */
export default function ChatPage() {
  return <ChatPageClient />;
}
